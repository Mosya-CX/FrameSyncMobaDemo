# FrameSyncMobaDemo -- Module Status

> Last updated: 2026-08-10 after the first live UOS two-client Gameplay run,
> its server/client log audit, and a final source audit that found the attempted
> UOS/LocalDirect NGO connection-owner correction incomplete: `Update()` is
> gated, but the NGO connection callback remains ungated.
> A clean compile alone is not treated as behavior verification.

## Validation baseline

| Validation | Current result |
|---|---|
| Unity | 2022.3.62f1c1, connected through Unity MCP |
| Compilation | Passing through Unity MCP after the 2026-08-10 `LocalNgoEndpointDriver` ownership-change attempt. Compilation success does not prove the race is closed. The final source audit found an ungated callback; later Console errors came from an invalid MCP asset-path inspection query, not from C# compilation. |
| EditMode | Historical broader baselines remain recorded below. Latest focused result: `ApplicationFlowTests.ClientConnectionLifecycle_HasExactlyOneOwnerPerFlowMode` 1/1 passed, but it only checks a helper return value and does not exercise `OnClientConnectedCallback`; it is insufficient behavior coverage for the live exception. No full assembly rerun is claimed for 2026-08-10. |
| PlayMode | Bootstrap PlayMode assembly 13/13 passed: GameScene full-match fixture (spawns, 2+1 waves, flow movement, tower selection/settlement, minion death, base-win closure), ClientBootstrap/ServerBootstrap -> Lobby scene transitions, online-flow override wiring, GameBootstrap real-asset composition, Lua pages (incl. catalog-driven hero list preload), UI prefabs and player-input simulation. |
| Multi-process | Local packaged Server + two Clients is accepted for the current flow. Live UOS also allocated a server and took two clients through identity, hero lock, GameScene, Loaded/Ready, StartTick 3 bootstrap and sustained Gameplay. One server UTP send-queue-full error and one client-side LocalNGO owner-race exception were found. The attempted source fix gates `Update()` but not `OnClientConnectedCallback`, so the client exception remains open. |
| Duplicate protocols | No duplicate UID, Command, Snapshot, Aim, AbilitySignal, Checksum or FixedPoint type found |

## Module matrix

| Module | Status | Current evidence / remaining limit |
|---|---|---|
| Deterministic foundation | **Verified** | `SimulationTickContext.Current` is the sole cross-system simulation-time source; active Tick ownership is tracked separately from its retained latest value. Project `fp`, stable UID/random/order and canonical serialization are integrated. |
| Runtime configuration | **Integrated for current full-match fixture** | Inspector float authoring bakes to fixed-point runtime tables; current Hero, Minion and Tower prototype/prefab mappings pass the real ClientBootstrap Catalog bake. Equipment and jungle fixture content remain later work. |
| Physics / movement / pathfinding | **Verified on the real Map fixture** | `PhysicsEntity2D` owns authoritative pose; Collider-aligned oriented-wall rasterization, grid-snapped lane skeletons, distance-graded flow convergence, exact blue/red waypoint chains, Direct/A*, radius-aware LOS, team flow fields and deterministic RVO passed focused EditMode/PlayMode tests. `Resources/Prefab/Map` owns map config, lanes, six baked fields and an Editor-only switchable visualizer that draws OwnerLane boundaries at full resolution; Bootstrap scene duplicates were removed. |
| Unit / lifecycle / composition | **Verified for seven formal prefabs** | Every formal Hero/Minion/Tower runtime prefab under `Assets/Config/Formal/Prefabs/` has exactly one root Unit, required Handlers, PhysicsEntity2D, Presentation host and eight semantic sockets. The obsolete `Assets/Resources/Prefab/Unit/` chain was removed during the 2026-08-10 resource consolidation. |
| Stats / XP | **Implemented** | Fixed-point runtime and authoring Bake are composed. |
| Combat / modifiers | **Verified** | Formal source headers, stable modifier formula/policy slots, shields, Attack and Projectile settlement pass focused tests. Kill/assist use a per-victim `CombatContributionEventLog` (D-035): every effective Damage/Shield/Heal is an event ordered by (LogicTick, SequenceInTick), the killer is the last Damage contributor, and assistants are distinct in-window enemy-hero Damage contributors sorted by UnitUid. Covered by `CombatContributionEventLogTests` (5), `AssistEventIntegrationTests` (2, incl. last-hit-not-highest-total), `CombatSystemTests`, `PassivePAbilityTests` and `BlightDetonationAndOnHitPassiveTests`. |
| Attack | **Implemented** | Begin/Commit/cancel/reset and successful-output sequence ownership use formal Combat source semantics. |
| Ability / PlayerInput | **Implemented** | Ability authoring, cost/stage validation, Focus/Commit, aim and canonical future-Tick Commands are integrated. Player input follows `MOBA_Player_Input_Command_Module_Design_v1_1`: `PlayerInputActions.inputactions` (Gameplay + UI maps, UI module rewired), UI-pointer click blocking (16.4), and presentation-only `ExpandStats` (hold C) via `PresentationInputState`. D-029: Varus test kit uses Commit+Point (E), Commit+Direction (R), HoldRelease+Direction (Q) and Toggle (W); `AbilityHandler` resolves Toggle/GroundTarget/VectorTarget stages and a second Commit turns a Toggle off without cooldown; the local indicator renders for `LocalAiming` and `GameplayFocusing` and is composed in GameScene (`SkillIndicatorDriver` + placeholder prefabs under `Assets/Resources/Prefab/Indicators/`). D-030: Q charge (ChargeRatio Blackboard + interpolated damage/range/AD ratio + W-empowered missing-health damage + -15%/hit piercing falloff with 33% floor via per-instance projectile on-hit override) and W blight (on-hit magic damage + stacking Blight Buff that detonates on Ability damage with hero CDR refund and per-stack cap vs monsters; toggled W is consumed by charging Q and starts 40s cooldown). D-031: per-level cooldowns (`AbilityDef.CooldownByLevel`), Q charge self-slow -20% (Blackboard StatModifierHandle) and timeout cancel + 50% mana refund (`HoldTimeoutPolicy.Cancel`), and passive P (fixed-passive wiring via `FixedPassiveDefinitionAsset`; Revenge Buff grants AS/AD/AP on kill, 3x on hero kills, level-scaled durations). All covered by EditMode tests. This feature phase raised the snapshot schema through v17; CrowdControl integration later raised the current repository schema to v18. The cast chase decision in `BehaviorPlanner.PlanCastIntent` reads `AbilityDef.CastRange` (zero = no range requirement) instead of a hardcoded range; covered by `PlanCastIntent_UsesAbilityCastRange_NotHardcoded`. |
| Buff / crowd control | **CC v6.2 runtime integrated (D-036); packaged validation fixed (2026-08-08)** | Buff core lifecycle works; HUD BuffBar display is Lua-driven (`UI.BuffCell`, stable `GetAllOrdered` view, design v14.2 UI rules). Crowd control was rebuilt to `moba_crowd_control_system_design_v6_2`: Definition/catalog/registry, module executor table (BlockActions, MaxMoveSlow, MaxAttackSpeedSlow, MinVisionScale, BasicAttackMiss, ForcedBehavior, ForcedMoveOnAdd, RemoveOnSignal, AddControlOnNaturalExpire), key params (`ControlParamKeys` + project `FixedBytes64`), immunity/cleanse/unstoppable, signals, Tenacity, unique forced move, forced behavior through BehaviorPlanner/Arbiter per Unit Framework v27.3. Standard definitions (Stun/Root/Slow/Silence/Disarm/KnockBack/Suppression/Sleep/Drowsy/Taunt/Charm/Fear) authored in `Assets/Config/Formal/CrowdControl/` and wired into GameScene. GameplaySnapshot schema 18. Packaged re-validation resolved: the baked hidden fields (`BakedParamLayout`/ops arrays) did not serialize because `CrowdControlParamLayoutEntry`/`ControlModuleOp` lacked `[Serializable]` and the readonly-struct `CrowdControlParamKey` was dropped by Unity's serializer; fixed by marking the structs serializable and storing the layout entry key as a serialized uint, then re-baking the 12 definition assets. Covered by `CrowdControlCatalogAssetTests` (catalog definitions baked/valid on disk + RegisterAll). Remaining: full Buff effect-module catalog (Buff side). |
| Projectile | **Implemented** | Deterministic motion/filter/order, Combat/Buff/CC dispatch, pooling and rollback state are integrated. |
| Equipment shop / gold | **Framework verified; scene catalog empty** | Runtime purchase/sell/undo tests exist, but GameBootstrap creates an empty `EquipmentDatabase` and no test equipment asset is present, so the current scene cannot exercise Shop. |
| Equipment active use | **Partial** | Command/codec/basic cooldown/charge execution exist. The current design names `EquipmentTargetPolicy` but defines no values or exact match contract, so target/range/NeedApproach arbitration is deliberately not invented. |
| Snapshot / rollback / checksum | **Implemented** | Current schema is 18 after CrowdControl snapshot integration; integrated future-affecting state uses strict restore and stable ordering, and the combat contribution event log participates in the shared checksum (D-035). Earlier focused FrameSync regressions passed; see the subsystem rows for the exact historical counts rather than assuming a full-suite rerun on 2026-08-10. |
| FrameSync authority / recovery | **Verified** | Canonical relay, authority history release, initial baseline, authority-before-prediction buffering, gap recovery and prediction limits passed focused tests and local process validation. |
| UOS / NGO application flow | **Live two-client Gameplay reached; startup defects remain** | The v10.2 scene split is live: ClientBootstrap -> Lobby -> GameScene and ServerBootstrap -> Lobby -> GameScene. The real Matchmaking config ID `f01c4e66-0023-43f6-af57-dcd8b73e7b90` is present in the Launcher-backed settings; the Multiverse startup/profile ID is separately `0fc730a2-ce02-4768-8a75-713ddb36c3b0` and must not be substituted for it. A live allocation reached Ready, accepted two distinct accounts, completed identity/hero/load barriers, applied StartTick 3 and ran Gameplay for minutes. Intended ownership is `LobbyFlowController` for UOS matchmaking/client connection/identity and `LocalNgoEndpointDriver` for the LocalDirect client lifecycle plus the server-side lobby path. Current source does not fully enforce that rule: `Update()` is gated, but the unconditionally subscribed `OnClientConnected()` callback still calls `NotifyClientConnectedOnce()` in UOS mode. The existing helper test is insufficient. The server also logged one UTP send-queue saturation error after the roughly 45 KB bootstrap burst; owner behavior, queue capacity and launch/HUD timing all require correction/rebuilt live validation. See `CURRENT_HANDOFF.md` and ExecPlan 0124. |
| Non-hero / TeamBase | **Verified for the current real-map minion/tower fixture** | Map-owned `MinionSpawns` feed three 2-melee + 1-ranged waves per team. Stable sub-kind priority, no-chase tower orders, projectile Combat settlement, formal minion death, base death and winning-team transition passed. Locked-target tower-projectile gating/red-line presentation and jungle content remain later work. |
| Non-hero / TeamBase (D-037) | **Tower mechanics + minion built-in buffs implemented** | Minion special mechanics are data-driven built-in buffs (`InitialBuffConfigIds` on prototypes; `BuffHandler.ApplyInitialBuffs`; CombatModifier target-kind/current-health extensions): `Buff_MinionMuncher` (+2% current health vs minions, melee), `Buff_MinionPincushion` (+3.5%, ranged), `Buff_TowerPillow` (towers x0.6). Minion growth dropped by user decision. `TowerAttackHandler : AttackHandler` adds hero ramp 180->x1.5/hit capped 600 and in-flight projectile locking (`HasUnresolvedProjectile`, NonHero v5 sections 9 and 8.5); ramp/lock state is snapshotted/checksummed; `TowerTargetLinePresenter` draws the red line. Test and formal tower prefabs migrated. Resource layout consolidated (2026-08-10): `Assets/Config/Formal/` is the single runtime chain; `Fixtures/`, the dead `Config/Formal/` pair, `Config/Runtime/`, `Config/FullMatchTest/` and `Resources/Prefab/Unit/` were removed, and all test scenes/tests now reference the Formal chain. Tests: MinionInitialBuffTests 4, TowerAttackHandlerTests 3, FrameSync 71, Bootstrap EditMode 58. |
| Presentation | **Formal Unit and tower binding verified** | Varus uses its available clips. Blue/red tower prefabs now use their authored Idle/Death clips; no attack animation is synthesized, so attacks correctly retain Idle. Presentation reads simulation time only through `SimulationTickContext.Current`. |
| UI / Lua | **Lua-driven pages verified; HUD coverage expanded** | `UIManager` owns the seven page prefabs (Main/Match/Select/Load/HUD/Shop/Result) with `UIPanel` + `luaModule` + `UIRefs`; page Lua (`Main/Match/Select/Load/HUD/Shop/Result`) and cells (`HeroCell`/`ShopCell`/`BuffCell`) run through `LuaManager`/`LuaHost`/`UIList`/`UICell`. Lua files live flat under `StreamingAssets/Lua/` with shared core in `Lua/Core/`; `LuaManager` strips a leading `UI.` module prefix and boots `Core.LuaInit`. PlayMode: `UiLuaPagesSmokeTests` 1/1, `UIManagerPrefabPlayModeTests` 2/2, `ClientFrameworkSmokeSceneTests` 1/1, `PlayerInputSimulationPlayModeTests` 3/3. The Select hero list is catalog-driven: `HeroDisplayTable` (avatar/name) auto-syncs rows from hero prototypes (D-028), `Select.lua` renders real heroes, `HeroCell.lua` sets the avatar, and the list is preloaded at Lobby entry; hero choice is unrestricted (duplicates allowed). HUD `luaModule` + 51 refs are wired (MatchPart, HeadIcon, Health/Mana Slider+Text, QWER icon+cooldown, Passive icon+cooldown, Gold, 16 property texts, Extend/Main property roots, 6 equipment slot buttons, BuffBar list); the six legacy C# HUD state controllers and `ScoreboardController` are deleted. HUD.lua drives vitals, cooldown masks, MatchPart scoreboard, stats, C-key expand panel, passive CD, equipment slots (click -> Shop focus) and the BuffBar. Player input is template-driven (`InputMappingTemplate` event-to-signal bindings; this version's default hold profile: press Focus / left Commit / release no-op; offline validation, no Bake). Main menu shows the resolved `TestAccountId` from the v10.2 auto-test account session. Remaining HUD gaps (production skill-icon sprites, skill-upgrade root, creep-score source, buff tooltips) and minimap (C# controller kept) remain pending user acceptance. |
| FrameSync / launch | **Implemented; live client timing observation open** | D-033 uses an absolute UTC barrier (`GameBootstrapPayload.LaunchUtcTicks`, wire v2). The live server timeline proves it honored the configured five-second delay: its Tick 1625 event occurred about 59 seconds after payload application, consistent with roughly 54 seconds of 30 Hz simulation. The operator nevertheless saw the client HUD open with match time near 30 seconds. Existing client logs lack timestamps at payload receive, barrier reach and HUD open, so queue delay, endpoint clock offset, main-thread delay and scheduling error cannot yet be distinguished. Do not revise D-033 from this observation alone; add narrow timeline diagnostics and rerun. Stat `Dirty` finalization/checksum diagnostics remain as previously verified. |

## Playable-logic closure audit

The bootstrap/network chain is closed both locally and through a real UOS
allocation far enough to reach sustained two-client Gameplay. This proves the
composition and authority path, but it does not close every production-facing
acceptance condition: startup queue capacity, the client Loading/HUD timeline,
result/return/remote settlement, Shop content and jungle content remain open.
Detailed continuation evidence is in `CURRENT_HANDOFF.md`.

## Remediation disposition

- No known code-side P0 remains in the already-tested bootstrap/network smoke chain.
- The UOS/LocalDirect connection-owner race is not yet fixed. The current
  `Update()` gate and helper test miss the NGO connection callback path; this is
  the first implementation item for the next task.
- The UTP startup send-queue saturation and the client Loading/HUD timing
  observation are open reliability/diagnostic work, not proven deterministic
  Gameplay corruption.
- ExecPlan 0125 P0/P1 findings (duplicate tower composition, non-persistent
  flow fields, and premature clearing of the shared Tick context) are fixed
  and focused-tested.
- Remaining full-match work includes locked-target tower-projectile/red-line
  conformance, jungle/equipment fixture content, final Lua behavior and
  operator visual acceptance.
- All code-side P1 items tracked by ExecPlan 0109 were repaired and focused-tested where the Unity Test Runner returned a result.
- P2 cleanup was limited to touched correctness paths; duplicate/dead shop planning code was removed.
- The incomplete `EquipmentTargetPolicy` design contract is an accepted non-blocking design underspecification, not a guessed production API.

## Permanent constraints

- Unit and Handlers remain prefab-authored MonoBehaviours.
- Inspector values may be `float`; authoritative Gameplay converts once and uses `fp`.
- Bootstrap owns Unity frame scheduling, NGO/UOS, scenes and UI; deterministic assemblies do not depend on those implementations.
- No production hero, concrete ability, Buff, equipment or map content is introduced by the remediation.
