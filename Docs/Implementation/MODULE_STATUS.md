# FrameSyncMobaDemo -- Module Status

> Document class: Current Module State
> Updated: 2026-08-22 against current source at base HEAD `60c84fd`, ExecPlan
> 0132-0136 evidence and retained Local C/S/UOS logs. Replace affected current
> rows; do not append a dated implementation history. A clean compile alone is
> not treated as behavior verification.

## Validation baseline

| Validation | Current result |
|---|---|
| Unity | 2022.3.62f1c1, connected through Unity MCP |
| Compilation | Passing through Unity MCP on 2026-08-20 after D-045 timing migration. Unity is idle and the Console contains no C# compilation error or exception. |
| EditMode | Full run on 2026-08-14: **877/887 passed, 10 failed**. Current Aatrox, Animator topology, FrameSync, equipment, camera and diagnostic coverage passed. The remaining failures are existing legacy expectations/fixtures in `BuffEffectLibraryTests` (1), `ChargeAbilityTests` (1), `CombatEnhancementTests` (1), `MapPathfindingAssetIntegrationTests` (1), `MovementIntegrationTests` (3), active-Tick guard tests (2) and `UnitAssemblyBoundaryTests` (1). They are retained openly rather than disabled. |
| PlayMode | Full run on 2026-08-14: **56/60 passed, 4 failed**. Current Aatrox, camera, pointer, tower-line, HeroTest and presentation coverage passed. Three old FrameworkSmoke tests still contain a SpawnPoint/team mismatch; one old `UnitPrefabCompositionPlayModeTests` fixture still uses pre-range PrefabId 9 instead of the formal Unit range `[1000, 1999]`. |
| Multi-process | cs28 accepted non-zero prices plus Purchase/Sell/Undo on the successful schema-22 Server/two-Client pair, with no Lua/C# exception, checksum mismatch or rejected Shop request. Logged CreepKills and the client balance prove the old binary did award 14/14/20 minion gold, but natural income obscured the increments. Source now logs confirmed UnitKill amount/total, uses integer allocation, formal initial 1500, minion 21/14 and hero 300; a matching rebuild and live reward-log acceptance remain pending. The server retains the separate known UTP send-queue warning, and the prior local C/S plus full UOS flow remain owner-accepted. |
| Duplicate protocols | No duplicate UID, Command, Snapshot, Aim, AbilitySignal, Checksum or FixedPoint type found |

## Module matrix

| Module | Status | Current evidence / remaining limit |
|---|---|---|
| Deterministic foundation | **Verified** | `SimulationTickContext.Current` is the sole cross-system simulation-time source; active Tick ownership is tracked separately from its retained latest value. Project `fp`, stable UID/random/order and canonical serialization are integrated. |
| Runtime configuration | **D-045 millisecond authoring integrated** | Reality-time Gameplay content is authored as integer milliseconds with explicit Ceil/Nearest/Floor policy, then bakes once to deterministic integer Tick. TickRate is validated in the 10..120 multiple-of-5 range; 20/30/60 conversion tests pass. Runtime/snapshot/checksum/command state remains Tick-only. The former 30 Hz direct-Tick values and equivalent times are recorded in `LEGACY_30HZ_TIME_AUTHORING_INVENTORY.md`. |
| Formal hero content / Aatrox | **Integrated and focused-tested** | Prototype `1002`, runtime prefab `1102`, passive/Q/W/E/R `10020`-`10024`, Buffs `12021`/`12022`/`12024`, W missile `109/2105`, stationary W area `110/2106`, Q VFX `3101`-`3103`, KnockUp `113`, English icons/text, animation profile and hero display row are registered. Q supports three exact sweet-spot impacts after 30 Ticks (1.0s), one-second minimum recast delays and four-second windows; its six normal/World-Ender clips are exactly 1.0s and the short lockout is visible through the HUD cooldown channel. Q/W key press opens local aim and primary click Commits; the runtime Q indicator reads the current Q1/Q2/Q3 StageDef, while W uses the standard direction bar. W has a 14-Tick (0.4667-second) deterministic windup, projectile delay and normal/World-Ender clip duration, tethers heroes only, owns its supplied area prefab as a snapshot-capable ProjectileWorld entity and pulls over 0.3s; E resets attack timing and extends terrain crossing up to 500 authored units; R fears non-heroes in 200 units for 1.5s and owns refreshable/decaying stat Buffs. The selection-independent editor Gizmos read actual Q/W authoring and expose user-controlled Off/All/Single modes. `AatroxAnimator.controller` has 26 states including Death and the supplied World Ender exit clip, uses the state-projection contract of `TestHeroRuntime`, and drives locked empowered attacks, blended passive-ready poses, World Ender and stage-specific normal/`_ULT` abilities through the generic profile. World Ender Buff `12024` controls the prefab's four wing/banner roots; outline baking occurs after those roots are hidden. HeroTestScene spawns Aatrox through the combined formal catalog. Large-monster W tethering is explicitly deferred. |
| Physics / movement / pathfinding | **Verified on the real Map fixture** | `PhysicsEntity2D` owns authoritative pose; Collider-aligned oriented-wall rasterization, grid-snapped lane skeletons, distance-graded flow convergence, exact blue/red waypoint chains, Direct/A*, radius-aware LOS, team flow fields and deterministic RVO passed focused EditMode/PlayMode tests. `Resources/Prefab/Map` owns map config, lanes, six baked fields and an Editor-only switchable visualizer that draws OwnerLane boundaries at full resolution; Bootstrap scene duplicates were removed. |
| Unit / lifecycle / composition | **Verified for the existing formal set plus Aatrox** | All eight current Hero/Minion/Tower runtime prefabs under `Assets/Resources/Prefab/Unit/` have been included in the Animator topology audit. Aatrox is derived from the established runtime hero composition, replaces only model/loadout/profile content, and passes focused prefab lifecycle tests. |
| Stats / XP | **Implemented; minion range conversion covered** | Fixed-point runtime and authoring Bake are composed. Minion experience share radius 800 is an authored/stat distance and is multiplied by `UnitWorld.StatDistanceToLogicDistanceScale` before squared logic-distance comparison; `MatchRewardDistanceTests` covers 7.99 inside / 8.01 outside at scale 0.01. |
| Kill rewards / gold income | **Integer settlement implemented; live rebuild pending** | Currency is integer end to end: `GoldAllocation.GoldAmount`, the Tick producer and `GoldIncomeRuntime` use `int`. Formal initialization is 1500; melee/ranged minions award the last-hitting hero 21/14; hero BaseGold is 300 with exact 3/5 killer share and stable assistant remainder split. `[GoldIncomeConfirmed]` logs confirmed UnitKill amount and resulting total. The Equipment v12 versus Combat v13.2 producer-owner conflict remains documented in D-041. |
| Combat / modifiers | **Verified** | Formal source headers, stable modifier formula/policy slots, shields, Attack and Projectile settlement pass focused tests. Kill/assist use a per-victim `CombatContributionEventLog` (D-035): every effective Damage/Shield/Heal is an event ordered by (LogicTick, SequenceInTick), the killer is the last Damage contributor, and assistants are distinct in-window enemy-hero Damage contributors sorted by UnitUid. Covered by `CombatContributionEventLogTests` (5), `AssistEventIntegrationTests` (2, incl. last-hit-not-highest-total), `CombatSystemTests`, `PassivePAbilityTests` and `BlightDetonationAndOnHitPassiveTests`. |
| Attack | **Implemented** | Begin/Commit/cancel/reset and successful-output sequence ownership use formal Combat source semantics. A ready passive with the generic empowered-basic-attack semantic and target-eligibility hook is resolved once at BeginAttack into the existing snapshotted/checksummed `IsEmpoweredAttack`; animation consumes only that locked read-only state. Aatrox's hook rejects Structure, matching its actual passive hit rule. |
| Ability / PlayerInput | **Implemented** | Ability authoring, cost/stage validation, Focus/Commit, aim and canonical future-Tick Commands are integrated. Player input follows `MOBA_Player_Input_Command_Module_Design_v1_1`: `PlayerInputActions.inputactions` (Gameplay + UI maps, UI module rewired), UI-pointer click blocking (16.4), and presentation-only `ExpandStats` (hold C) via `PresentationInputState`. D-029: Varus test kit uses Commit+Point (E), Commit+Direction (R), HoldRelease+Direction (Q) and Toggle (W); `AbilityHandler` resolves Toggle/GroundTarget/VectorTarget stages and a second Commit turns a Toggle off without cooldown; the local indicator renders for `LocalAiming` and `GameplayFocusing` and is composed in GameScene (`SkillIndicatorDriver` + placeholder prefabs under `Assets/Resources/Prefab/Indicators/`). E's serialized `UnitTargetFilter` is now enemy/alive/targetable with `Structure` included, and `AreaDamageStageDef` supplies the caster's real TeamId to RangeQuery instead of Neutral. D-030: Q charge (ChargeRatio Blackboard + interpolated damage/range/AD ratio + W-empowered missing-health damage + -15%/hit piercing falloff with 33% floor via per-instance projectile on-hit override) and W blight (on-hit magic damage + stacking Blight Buff that detonates on Ability damage with hero CDR refund and per-stack cap against every non-hero UnitKind, including Structure; toggled W is consumed by charging Q and starts 40s cooldown). D-031 remains as previously implemented. Structure behavior is covered by formal-config and runtime EditMode tests. |
| Buff / crowd control | **CC v6.2 runtime integrated (D-036); packaged validation fixed (2026-08-08)** | Buff core lifecycle works; HUD BuffBar display is Lua-driven (`UI.BuffCell`, stable `GetAllOrdered` view, design v14.2 UI rules). Crowd control was rebuilt to `moba_crowd_control_system_design_v6_2`: Definition/catalog/registry, module executor table (BlockActions, MaxMoveSlow, MaxAttackSpeedSlow, MinVisionScale, BasicAttackMiss, ForcedBehavior, ForcedMoveOnAdd, RemoveOnSignal, AddControlOnNaturalExpire), key params (`ControlParamKeys` + project `FixedBytes64`), immunity/cleanse/unstoppable, signals, Tenacity, unique forced move, forced behavior through BehaviorPlanner/Arbiter per Unit Framework v27.3. Standard definitions are authored in `Assets/Config/Formal/CrowdControl/` and wired into GameScene. A presentation-only wrapper now gives KnockUp/KnockBack duration-derived model Y arcs, with KnockBack at half peak; Gameplay remains planar and deterministic. Current GameplaySnapshot schema is 22. ExecPlan 0132 adds the formal 3-second/four-stack Seething Strike Buff through the existing stack-scaled stat modifier path. Remaining: the broader Buff effect-module catalog. |
| Projectile | **Implemented** | Deterministic motion/filter/order, Combat/Buff/CC dispatch, pooling and rollback state are integrated. Optional prefab-baked trapezoid containment metadata supports stationary area projectiles; Buff-owned projectile UIDs are stored as deterministic slots and pending/active projectiles can be explicitly ended. |
| Equipment shop / gold | **First formal content slice implemented (D-039/D-040); packaged composition corrected in source** | GameScene and HeroTestScene's driver both load `FormalEquipmentCatalog` with Dagger, Amplifying Tome, Pickaxe, Recurve Bow and Guinsoo's Rageblade. GameScene's composition root bakes that catalog into the sole `UnitWorld.EquipmentDatabase`; `EquipmentShopRuntime` and the Lua Shop enumerate that database. HeroTest has no network transport but composes the same shop/gold runtime, starts from a 10000-gold initialization baseline, and submits canonical shop Commands to its local Tick pipeline. Before the first successful transaction, UI price and RequestCheck resolve the controlled hero through the stable PlayerSlot mapping without creating Trader state; a successful Purchase/Sell command creates Trader state lazily as required by v12. Formal clients inject the existing `PlayerCommandRequester` as `IEquipmentShopCommandSubmitter` at completed local-player binding, so UI actions enter the same canonical FrameSync command collector. `[ShopRequest]` logs expose slot/gold/price/result per action. Dynamic recipe prices, component consumption, purchase/sell/undo, Basic/Advanced duplicates, Finished-item rejection and the six-slot owned-equipment view use formal runtime state. Recurve/Guinsoo On-Hit damage, Seething Strike stacks and the every-third full-stack repeat remain data-driven. |
| Equipment active use | **Partial** | Command/codec/basic cooldown/charge execution exist. The current design names `EquipmentTargetPolicy` but defines no values or exact match contract, so target/range/NeedApproach arbitration is deliberately not invented. |
| Snapshot / rollback / checksum | **Implemented, schema 22** | Equipment module state is passed by `ref`; `TriggerCount` is captured/restored, compared in transaction state and included in both checksum writers. Focused Restore/Resolve/replay and checksum-difference tests pass. All endpoints must use the same rebuilt schema-22 package. |
| FrameSync authority / recovery | **Verified** | Canonical relay, authority history release, initial baseline, authority-before-prediction buffering, gap recovery and prediction limits passed focused tests and local process validation. |
| UOS / NGO application flow | **D-045 network-monotonic launch implemented; rebuilt UOS acceptance pending** | The two-phase SceneLoaded/Bootstrap/BootstrapApplied/all-client barrier/LaunchCommit flow is retained. Launch authorization now uses integer milliseconds in NGO synchronized-server-time domain; local monotonic time paces loading and post-threshold simulation. OS UTC no longer authorizes launch. Timestamp lateness cannot manufacture Gameplay backlog: client catch-up is bounded by continuous AuthorityFrames, prediction lead and per-Unity-frame Tick limits. GameplayDataVersion is 3; launch wire v2 and bootstrap payload wire v3 reject mixed packages. |
| Non-hero / TeamBase | **Verified for the current real-map minion/tower fixture** | Map-owned `MinionSpawns` feed three 2-melee + 1-ranged waves per team. Stable sub-kind priority, no-chase tower orders, projectile Combat settlement, formal minion death, base death and winning-team transition passed. Tower projectile gating is deterministic; the red line is client presentation-only, follows the current `AttackTarget`, switches immediately to its successor and hides for a dead/invalid target when no successor exists. Jungle content remains later work. |
| Non-hero / TeamBase (D-037) | **Tower mechanics + minion built-in buffs implemented** | Minion special mechanics are data-driven built-in buffs (`InitialBuffConfigIds` on prototypes; `BuffHandler.ApplyInitialBuffs`; CombatModifier target-kind/current-health extensions): `Buff_MinionMuncher` (+2% current health vs minions, melee), `Buff_MinionPincushion` (+3.5%, ranged), `Buff_TowerPillow` (towers x0.6). Minion growth dropped by user decision. `TowerAttackHandler : AttackHandler` adds hero ramp 180->x1.5/hit capped 600 and in-flight projectile locking (`HasUnresolvedProjectile`, NonHero v5 sections 9 and 8.5); ramp/lock state is snapshotted/checksummed; `TowerTargetLinePresenter` draws the red line. Current unit assets live under `Assets/Resources/Prefab/Unit/`; both minion teams use direct Idle/Move transitions and both tower teams correctly have no locomotion states. Tests: MinionInitialBuffTests 4, TowerAttackHandlerTests 3, FrameSync 71, Bootstrap EditMode 58, plus the all-unit Animator topology audit. |
| Presentation | **Formal camera/pointer, Aatrox and tower binding verified** | Varus uses its available clips. Aatrox's generic `UnitAnimationProfile` maps attack snapshot, passive-ready state, World Ender Buff presence and Q/W/E/R stage keys to a 26-state/15-parameter Controller including `AatroxDeath` and `AatroxULTOut`; all six Q clips are 1.0s and both W variants are 14/30s. Passive-ready locomotion and empowered-attack entry use visible blends, while the generic animation-variant exit trigger plays the supplied World Ender exit clip. Its six normal/passive-ready/World-Ender Idle/Walk states form a complete direct graph with no AnyState locomotion entry, self transition or persistent-condition walk restart. Formal camera follow samples the same presentation-smoothed `PhysicsEntity2D` root timeline; interpolation stays exclusively in the existing LateUpdate Transform write point and never changes logic pose. `MobaCameraPresentationConfig` owns opposite blue/red views, VSync, pointer radius/colors and smoothing. `CameraDebugScene` is a saved small formal-scene analogue with no grid/pathfinding/network/Gameplay Unit runtime; its fixed-point lightweight proxy harness reports pointer error, candidate count and detection/outline timings and offers optional 128/512-proxy stress generation. `ClientUnitOutline` repeats one local outline material across every source submesh and now bakes after Buff-driven wing hiding, preventing hidden wings from remaining outlined. Generic CC model arcs remain presentation-only. Blue/red tower prefabs use authored Idle/Death clips; no attack animation is synthesized. Presentation reads simulation time only through `SimulationTickContext.Current`. |
| UI / Lua | **Lua-driven pages verified; Shop/HUD equipment views corrected** | `UIManager` owns the seven page prefabs and Lua hosts. Shop cells now bind the catalog Sprite, display the formal dynamic purchase price and retain selection through visual owned masks (the complete mask subtree is non-raycast, so purchased cells remain clickable). Selecting an owned catalog entry resolves its lowest occupied slot for details and selling; HUD reads all six equipment slots from `EquipmentHandler`; HeroTest HUD and Shop read the same formal balance instead of the former fake `999999`; HUD and an open Shop refresh from the same post-Tick state so component-consumption marks, recipe discounts and Undo availability do not remain stale. All newly authored equipment and Seething Strike visible text is English because the current TMP font does not provide CJK glyphs. The existing catalog-driven Select, HUD vitals/cooldowns/stats/BuffBar, template-driven player input and minimap boundaries are unchanged. Remaining HUD gaps are production skill-icon sprites, skill-upgrade root, creep-score source and buff tooltips. |
| FrameSync / launch | **D-045 implemented; live rebuilt UOS validation pending** | Bootstrap owns the two-phase launch schedule; deterministic `FrameSyncGameRuntime` owns no wall-clock timestamp. `LaunchCommit` carries NGO synchronized server-time milliseconds, while local pacing uses monotonic milliseconds. A late commit starts from its local threshold/receive anchor and only true continuous AuthorityFrame backlog can authorize bounded catch-up. Focused RuntimeConfig, launch codec/schedule, Bootstrap and FrameSync EditMode suites pass. |

## Playable-logic closure audit

The bootstrap/network chain is closed both locally and through a real UOS
allocation far enough to reach sustained two-client Gameplay. This proves the
composition and authority path, but it does not close every production-facing
acceptance condition: startup queue capacity, the client Loading/HUD timeline,
result/return/remote settlement, Shop content and jungle content remain open.
Detailed continuation evidence is in `CURRENT_HANDOFF.md`.

## Remediation disposition

- No known code-side P0 remains in the already-tested bootstrap/network smoke chain.
- The retained UOS package contained one LocalNGO notification exception, but
  current source subscribes that callback only from the LocalDirect
  `StartClient()` path. Add behavior coverage before claiming the old callback
  remains reachable; a defensive method-level guard is still reasonable.
- The UTP startup send-queue saturation and the client Loading/HUD timing
  observation are open reliability/diagnostic work, not proven deterministic
  Gameplay corruption.
- D-045 source/formal-asset work is complete and focused-tested. Matching rebuilt
  Local C/S and UOS live acceptance is a separate pending request.
- The retained full-suite baseline has 10 EditMode and 4 PlayMode failures;
  these remain visible until their fixtures/expectations are repaired.
- Remaining product work includes jungle content, production presentation/UI
  assets, result/return/remote settlement acceptance and operator visual
  acceptance.
- The incomplete `EquipmentTargetPolicy` design contract is an accepted non-blocking design underspecification, not a guessed production API.

## Permanent constraints

- Unit and Handlers remain prefab-authored MonoBehaviours.
- Reality-time Inspector values use integer milliseconds; other numeric authoring may use `float`, and authoritative Gameplay converts once to Tick/`fp`.
- Bootstrap owns Unity frame scheduling, NGO/UOS, scenes and UI; deterministic assemblies do not depend on those implementations.
- Framework runtime types remain generic; the user-requested Guinsoo equipment
  chain is the explicit formal content exception recorded by D-039.
