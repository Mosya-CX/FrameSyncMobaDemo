# FrameSyncMobaDemo -- Module Status

> Document class: Current Module State
> Updated: 2026-08-31 against current source, ExecPlans 0138/0140/0141/0142/0148/0149/0150/0151/0152/0153/0154 evidence and
> retained Local C/S/UOS logs. Replace affected current
> rows; do not append a dated implementation history. A clean compile alone is
> not treated as behavior verification.

## Validation baseline

| Validation | Current result |
|---|---|
| Unity | 2022.3.62f1c1, connected through Unity MCP |
| Compilation | Clean Unity MCP forced script refresh passed on 2026-08-31 after ExecPlan 0154; the Editor is idle and the final isolated Console Error query is empty after the focused runs. The client-only Bootstrap PlayMode assembly is constrained away from the current `UNITY_SERVER` target, which removes the pre-existing server-target reference errors. Linux Dedicated Server and ordinary Player subtarget build evidence from 2026-08-23 predates the new logic Addressables partitions and requires the pending corrected rebuild. |
| EditMode | D-052 animation sampling and match-isolated clock are **13/13**, formal Animator asset contracts are **6/6**, and PlayerInput is **42/42**. The latter includes the exact predicted Focus -> rollback replay absence -> Idle -> strictly later accepted CommandSeq -> authority execution -> primary-click Commit sequence, the accepted-after-completed-Tick callback-order regression, plus negative same-Tick/untracked/slot/verb/sequence guards. The focused formal-death, Despawn and Q route/Commit cases are **6/6**. After ExecPlan 0154, full FrameSync is **123/123** and full Bootstrap EditMode is **123/123**, including match-scoped duplicate suppression, owner-invalidation ordering, adjacent sequences, client send gating and VFX preload/reuse/filter coverage. The full Unit probe remains **553 passed / 10 retained failures**, the same Buff (1), charge (1), combat enhancement (1), authored lane (1), movement (3), active-Tick guards (2) and assembly-boundary (1) categories. Earlier Deterministic **53/53** and RuntimeConfig Editor **47/47** evidence remains unchanged. |
| PlayMode | D-052 Aatrox controller routing, bound Unit/View/Driver change-frame routing and external loop-Motion-Time pose sampling are **9/9**; shared presentation-config application is **3/3**. Generic indicator atomic generation replacement, the existing source-material/world-space/framebuffer test and W-then-Q input/indicator simulation each pass **1/1**; the generation test replaces Direction/Range/Ground instances and material clones before releasing old leases and verifies Aatrox Q's independent line path after rebind. Representative Addressables acquire/release passes **1/1**. The broad Bootstrap probe retains its existing lifecycle/fixture failures; this run reproduced ClientComposition's 600-frame async timeout and the undeclared destroy/load cancellation exception. The broad ClientContent namespace also retains the unrelated missing Projectile prefab 2101 fixture. |
| Multi-process | cs28 accepted non-zero prices plus Purchase/Sell/Undo on the successful schema-22 Server/two-Client pair, with no Lua/C# exception, checksum mismatch or rejected Shop request. Logged CreepKills and the client balance prove the old binary did award 14/14/20 minion gold, but natural income obscured the increments. Source now logs confirmed UnitKill amount/total, uses integer allocation, formal initial 1500, minion 21/14 and hero 300; a matching rebuild and live reward-log acceptance remain pending. The server retains the separate known UTP send-queue warning, and the prior local C/S plus full UOS flow remain owner-accepted. |
| Duplicate protocols | No duplicate UID, Command, Snapshot, Aim, AbilitySignal, Checksum or FixedPoint type found |

## Module matrix

| Module | Status | Current evidence / remaining limit |
|---|---|---|
| Deterministic foundation | **Verified** | `SimulationTickContext.Current` is the sole cross-system simulation-time source; active Tick ownership is tracked separately from its retained latest value. Project `fp`, stable UID/random/order and canonical serialization are integrated. |
| Runtime configuration | **D-045 millisecond authoring integrated** | Reality-time Gameplay content is authored as integer milliseconds with explicit Ceil/Nearest/Floor policy, then bakes once to deterministic integer Tick. TickRate is validated in the 10..120 multiple-of-5 range; 20/30/60 conversion tests pass. Runtime/snapshot/checksum/command state remains Tick-only. The former 30 Hz direct-Tick values and equivalent times are recorded in `LEGACY_30HZ_TIME_AUTHORING_INVENTORY.md`. |
| Match-scoped Addressables / server resources | **D-051 source/assets/focused tests complete; Player rebuild pending** | `GlobalPrefabTable` remains the sole PrefabId authority but is now an empty root index over Core, Map 1, Varus and Aatrox path-only child tables. The four `Logic-*` groups contain 35 deterministic roots; client presentation retains 63 roots across eight `Client-*` groups and zero remote entries. Lobby freezes the complete map/hero closure before `GameScene`; Bootstrap loads, version/hash-validates, canonicalizes and freezes only selected partitions before initial Snapshot/Tick 0, then exposes the resolved synchronous table to Gameplay. `GameScene` has no direct Unit/Ability/Projectile/Buff/CC/Equipment/map catalog references. Core is the sole serialized owner of the shared Unit dispose-policy table; Hero Unit catalogs carry no policy reference, avoiding cross-bundle Unity-object identity conflicts while preserving strict combined-bake validation. Client builds include Logic plus Client; Dedicated Server now builds a local catalog with only Logic groups, validates the transitive dependency graph and rejects any client bundle, while `FrameSyncMoba.ClientContent` remains excluded. Match scope and view leases own/release handles exactly; indicator generations additionally retain the old leases until every live driver has synchronously rebuilt from the fully acquired new generation, then release the old generation. Addressables never enters Tick, Snapshot, checksum or rollback state. The editor-only `HeroTestDriver` now resolves the selected Core/map/hero child tables before baking its local test world; the Varus `1001/1101` scene smoke run passed without changing production loading. Bootstrap EditMode is 118/118 and focused scoped-load/GameBootstrap PlayMode is 4/4. Final Windows/Linux Player build and report inspection remain user-owned. See `Docs/Implementation/Addressables/RESOURCE_ARCHITECTURE.md`. |
| Formal hero content / Aatrox | **Integrated and focused-tested** | Prototype `1002`, runtime prefab `1102`, passive/Q/W/E/R `10020`-`10024`, Buffs `12021`/`12022`/`12024`, W missile `109/2105`, stationary W area `110/2106`, Q VFX `3101`-`3103`, KnockUp `113`, English icons/text, animation profile and hero display row are registered. Q supports three exact sweet-spot impacts after one authored second baked to the configured Gameplay TickRate, one-second minimum recast delays and four-second windows; its six normal/World-Ender clips are exactly 1.0s and each Q VFX now scales from `ImpactDelayTicks / World.TickRate`, so changing the offline TickRate does not desynchronize effect and animation. Q/W key press opens local aim and primary click Commits; the runtime Q indicator reads the current Q1/Q2/Q3 StageDef, while W uses the standard direction bar. W retains its authored deterministic windup, projectile delay and normal/World-Ender clip duration, tethers heroes only, owns its supplied area prefab as a snapshot-capable ProjectileWorld entity and pulls over 0.3s; E resets attack timing and extends terrain crossing up to 500 authored units; R fears non-heroes in 200 units for 1.5s and owns refreshable/decaying stat Buffs. The selection-independent editor Gizmos read actual Q/W authoring and expose user-controlled Off/All/Single modes. `AatroxAnimator.controller` has 26 states including Death and the supplied World Ender exit clip, uses the state-projection contract of `TestHeroRuntime`, and drives locked empowered attacks, blended passive-ready poses, World Ender and stage-specific normal/`_ULT` abilities through the generic profile. World Ender Buff `12024` controls the prefab's four wing/banner roots; outline baking occurs after those roots are hidden. `HeroTestScene` is currently switched to Varus for W/Q acceptance; Aatrox remains available through the combined formal catalog. Large-monster W tethering is explicitly deferred. |
| Physics / movement / pathfinding | **Verified on the real Map fixture** | `PhysicsEntity2D` owns authoritative pose; its client projection now interpolates position and rotation on independent clocks, so frequent facing changes cannot restart position progress or shake an exactly-following locked camera. Collider-aligned oriented-wall rasterization, grid-snapped lane skeletons, distance-graded flow convergence, exact blue/red waypoint chains, Direct/A*, radius-aware LOS, team flow fields and deterministic RVO passed focused EditMode/PlayMode tests. The formal logic map prefab owns map config, lanes and six baked fields; the Addressable `view/map/main` root owns client rendering. Bootstrap scene duplicates were removed. |
| Unit / lifecycle / composition | **D-047 structured arbitration integrated; logic/view split complete** | Intent replacement is Unit-owned; Planner only proposes. Arbiter is the sole ordinary action-start/cancel boundary and uses structured results plus the frozen resource matrix, while fixed Main/Base Runtime slots own lifecycle reservations. Spec resolution, Handler dispatch and Runtime reconciliation are separate internal services. Frozen formal deaths and non-death Despawn now invalidate dependent uncommitted attacks through stable UnitWorld traversal and one Arbiter call that updates Handler and Main together; rollback topology removal remains separate. The old no-Planner direct-Handler Pipeline path was removed; missing Planner/Arbiter composition fails visibly. Handler-owned automatic/signal Stage transitions are re-described in the same Tick. All eight formal Unit prefabs now contain deterministic logic only; their render/Animator/outline/socket trees are asynchronous client views keyed through the same PrefabId table. |
| Stats / XP | **Implemented; 1.5x minion range covered** | Fixed-point runtime and authoring Bake are composed. Minion experience share radius is now 1200 authored/stat distance and is multiplied by `UnitWorld.StatDistanceToLogicDistanceScale` before squared logic-distance comparison; `MatchRewardDistanceTests` covers 11.99 inside / 12.01 outside at scale 0.01. |
| Kill rewards / gold income | **Integer settlement implemented; live rebuild pending** | Currency is integer end to end: `GoldAllocation.GoldAmount`, the Tick producer and `GoldIncomeRuntime` use `int`. Formal initialization is 1500; melee/ranged minions award the last-hitting hero 21/14; hero BaseGold is 300 with exact 3/5 killer share and stable assistant remainder split. `[GoldIncomeConfirmed]` logs confirmed UnitKill amount and resulting total. The Equipment v12 versus Combat v13.2 producer-owner conflict remains documented in D-041. |
| Combat / modifiers | **D-049/D-050 fairness + D-054 structure admission complete** | D-049 global Handler subphases, sealed causal waves, fixed-point conserving Shield/Heal/Damage allocation, synchronous formal death, scheme-A killer authority and scheme-C exact-killer-tie fallback remain intact. D-050 adds immutable participant/action/effect identities: probabilistic Crit hashes match seed + `OriginActionId` + target `GameplayParticipantId` + `EffectOrdinal` without touching the global random stream; production attacks, abilities, projectiles, Buff/equipment reactions and Deferred Damage carry the provenance. D-054 now admits external Structure damage only for the canonical `Attack + BasicAttack` source pair and rejects other Damage, Heal, Shield, Buff, CrowdControl and direct tag effects at central admission boundaries while allowing structure self effects. Deferred rejections happen before sequence allocation and Snapshot storage. Varus W Blight/bonus damage and equipment on-hit additions no longer affect towers; ordinary and empowered basic-attack body damage still does. Event-derived damage folds the parent ordinal into a path-stable child key; negative ordinals fail at submission/Deferred/Restore boundaries. Missing probabilistic identity fails deterministically. UID remains entity/serialization authority and a complete hash-collision fallback. The full Unit regression is `570 passed / 10 retained baseline failures`; focused structure policy is `14/14`. |
| Attack | **Implemented; D-047 control path verified** | Begin/Commit/cancel/reset and successful-output sequence ownership use formal Combat source semantics. Attack windup occupies MainRuntime. If its target formally dies or is despawned before Commit, ActionArbiter atomically calls `CancelBeforeCommit` and releases Main; a mismatched half-state fails visibly. An already committed attack is not revoked and retains its Ready Tick plus emitted damage/projectile ownership. Forced-behavior Attack bypasses only the voluntary capability veto, remains subject to AbilityMask/fine control/target/range/readiness checks, and stays active under a `VoluntaryAttack`-only block. Aatrox empowered-attack behavior is unchanged. Aatrox and both melee-minion logic Prefabs use direct basic-attack settlement (`ProjectileDefId = 0`); Varus retains projectile `101`. Ranged classification remains derived from `AttackRange` against the configured threshold, independently of projectile presentation/settlement. |
| Ability / PlayerInput | **D-053 adaptive TargetTick + D-054 structure exclusion verified** | Existing ability/input content remains integrated. D-053 samples integer SRTT/RTT variation from Ping v2's server-Tick anchor and selects one bounded TargetTick per local build Tick; connected clients warm samples during the loaded/ready launch barrier, while cold-start/stale timing retains the v10.2 static formula and Command/Snapshot bytes are unchanged. AbilityHandler exposes structural requested/active Stage descriptions while retaining cost/cooldown/session authority. Aatrox Q Main cast coexists with authored E Base dash; Varus Q Hold coexists with route Move, while manual or automatic Release reclaims Facing and cancels that Move. Varus formal E and ability-projectile masks now exclude Structure, with central D-054 admission as the safety net. Pure Toggle W remains attack-neutral, but its bonus AttackEffect and Blight cannot affect a tower. Accepted-Tick reconciliation, pending indicators and generic HeroTest/GameScene input composition are unchanged. Target estimator/resolver is `12/12`, PlayerInput EditMode is `42/42`, formal Varus structure assets are `7/7`, and PlayerInput simulation PlayMode is `5/5`. |
| Buff / crowd control | **CC v6.2 runtime integrated; D-047 forced actions verified** | Existing module/catalog/immunity/cleanse/forced-move behavior is retained. Forced Move and Taunt Attack traverse Planner/Arbiter without being rejected by the voluntary coarse capability state, while fine `ControlMove`/`ControlAttack` blocks still win. Event-derived damage retains the originating action identity and folds the parent effect ordinal into its child key. GameplaySnapshot schema is 24. Remaining: the broader Buff effect-module catalog. |
| Projectile | **Implemented; D-050 neutral ties verified** | Deterministic motion/filter/order, Combat/Buff/CC dispatch, pooling and rollback state are integrated. Pending/active projectiles preserve `OriginActionId`; pending tracked spawns also preserve `TargetUnitUid` through Restore. Moving and AoE candidates remain distance-first, then use match-seeded gameplay-participant scoring for exact distance ties. Hit limits, end-on-first and piercing therefore select the same participant after technical UID relabeling. Optional containment metadata, explicit ending and resident client view leases remain unchanged. |
| Equipment shop / gold | **First formal content slice implemented (D-039/D-040); packaged composition corrected in source** | GameScene and HeroTestScene's driver both load `FormalEquipmentCatalog` with Dagger, Amplifying Tome, Pickaxe, Recurve Bow and Guinsoo's Rageblade. GameScene's composition root bakes that catalog into the sole `UnitWorld.EquipmentDatabase`; `EquipmentShopRuntime` and the Lua Shop enumerate that database. HeroTest has no network transport but composes the same shop/gold runtime, starts from a 10000-gold initialization baseline, and submits canonical shop Commands to its local Tick pipeline. Before the first successful transaction, UI price and RequestCheck resolve the controlled hero through the stable PlayerSlot mapping without creating Trader state; a successful Purchase/Sell command creates Trader state lazily as required by v12. GameScene and HeroTest both inject one `PlayerCommandRequester` as `IEquipmentShopCommandSubmitter`, so Shop, QWER, Move, Attack and skill allocation share one canonical collector and CommandSeq owner. `[ShopRequest]` logs expose slot/gold/price/result per action. Dynamic recipe prices, component consumption, purchase/sell/undo, Basic/Advanced duplicates, Finished-item rejection and the six-slot owned-equipment view use formal runtime state. Recurve/Guinsoo On-Hit damage, Seething Strike stacks and the every-third full-stack repeat remain data-driven. |
| Equipment active use | **Partial** | Command/codec/basic cooldown/charge execution exist. The current design names `EquipmentTargetPolicy` but defines no values or exact match contract, so target/range/NeedApproach arbitration is deliberately not invented. |
| Snapshot / rollback / checksum | **Implemented, schema 24; live mismatch diagnostics expanded** | Unit Snapshot/checksum includes unique immutable `GameplayParticipantId`; Combat Deferred Damage includes action/effect provenance; pending/active Projectile Snapshot/checksum includes `OriginActionId`. Restore rejects missing/duplicate participant IDs and reconciles relabeled technical UIDs without transient identity duplication. After `CombatSystem.EndTick`, frozen deaths atomically clear every matching uncommitted Attack Handler/Main pair before Tick-end capture. Two new predicted-command replacement/frozen-anchor replay regressions pass, including preservation of a future local command beyond the correction replay boundary, but they do not reproduce the supplied Client-A Tick 5152 mismatch. With D-032's explicit `-checksumDetail` flag, the server emits per-Tick aggregate segments plus Unit identity/pose/intent/Main/Base/attack/locomotion, projectile, pending lifecycle and minion-wave state; the client emits the same structure on mismatch. No Snapshot member, checksum membership, schema, Restore boundary or wire contract changed. All endpoints require one matching schema-24/GameplayDataVersion-4/bootstrap-wire-4 rebuild. |
| FrameSync authority / recovery | **Verified; application-command idempotency corrected** | Canonical relay, authority history release, initial baseline, authority-before-prediction buffering, gap recovery and prediction limits passed focused tests and local process validation. Accepted network command identity is match-scoped `ClientId + CommandSeq`: duplicates are discarded before late retargeting and authorization, but distinct adjacent sequences both execute. This ledger is application state only and does not enter Snapshot/checksum/Gameplay ordering. Full FrameSync is `121/121`. |
| UOS / NGO application flow | **D-045 launch, 0152 send gating and 0157-0160 signed Player distribution/build tooling implemented; live build/CDN acceptance pending** | The two-phase launch flow and canonical command-send gating are unchanged. ExecPlans 0157-0159 add only a standalone Launcher-managed Player file channel: UOS Bucket/Release/Badge routes a detached RSA-SHA256 schema-v3 manifest, and every remote payload is a sub-100-MB `content/<sha256>` Entry. Empty installs reconstruct the complete ZIP; trusted older installs reuse unchanged files and download only changed content. ExecPlan 0160 adds an isolated non-Development Unity release build at `Builds/Demo/Game/AAALOL.exe`; its post-build schema-v3 packaging checkbox is optional and disabled by default, while `Builds/UosClient` remains the independent full test client. D-050 GameplayDataVersion 4, Snapshot schema 24, launch wire v2, bootstrap payload wire v4 and D-051 local Addressables remain unchanged. Actual Player build, UOS upload, Badge propagation and external download acceptance remain user-owned. |
| Formal Demo launcher | **0157-0159 signed full install, file-level incremental update and flat sub-100-MB CDN content implemented** | `Tools/UosGameLauncher` remains a standalone .NET 8 WinForms launcher at `Builds/Demo/Launcher`, now version 1.3.1 and published self-contained. Its only primary button explicitly switches among download, update, start, cancel and stop states. Download/update completion never launches the Player; Start rechecks the signed remote state and can return to download/update before any process is created. The Launcher supports Range resume per content Entry, complete file/ZIP hashes, staged atomic swap and trusted recovery. The upload root contains only two manifest files plus `content`; no physical or logical `packages`, `objects` or `chunks` paths remain. Release build and loopback state/full-install/incremental/corruption/recovery self-test pass; the current 1.0.0 package contains 277 logical files and 284 physical content Entries, with no upload file over 95,000,000 bytes. Actual player-machine UI acceptance remains pending. See `Docs/Implementation/GAME_LAUNCHER_GUIDE.md`. |
| Non-hero / TeamBase | **Tower combat and D-054 external-effect filter verified** | Map-owned `MinionSpawns` feed three 2-melee + 1-ranged waves per team. Stable sub-kind priority, no-chase tower orders, projectile Combat settlement, formal minion death, base death and winning-team transition passed. Structures now accept external damage only from the canonical basic-attack source pair and reject ability/on-hit/equipment/Buff/heal/shield/control/tag effects; self-owned effects remain legal. Deferred requests are filtered before deterministic sequence/Snapshot mutation. The Varus Blight presentation PlayMode probe confirms a rejected tower Buff creates zero stack marks. Tower projectile gating and target-line presentation remain unchanged. Jungle content remains later work. |
| Non-hero / TeamBase (D-037) | **Tower mechanics + minion built-in buffs implemented** | Minion special mechanics are data-driven built-in buffs (`InitialBuffConfigIds` on prototypes; `BuffHandler.ApplyInitialBuffs`; CombatModifier target-kind/current-health extensions): `Buff_MinionMuncher` (+2% current health vs minions, melee), `Buff_MinionPincushion` (+3.5%, ranged), `Buff_TowerPillow` (towers x0.6). Minion growth dropped by user decision. `TowerAttackHandler : AttackHandler` adds hero ramp 180->x1.5/hit capped 600 and in-flight projectile locking; ramp/lock state is snapshotted/checksummed. Formal minion/tower logic prefabs live under `Assets/Config/Formal/Prefabs/Logic/Unit/`; client models, Animator and `TowerTargetLinePresenter` live in Addressable Unit views. Both minion teams use direct Idle/Move transitions and both tower teams have no locomotion states. |
| Presentation | **D-052 interpolation + VFX warmup verified in source/focused tests** | Varus uses its available clips. Aatrox's generic `UnitAnimationProfile` maps attack snapshot, passive-ready state, World Ender Buff presence and Q/W/E/R stage keys to a 26-state/16-parameter Controller including `AatroxDeath` and `AatroxULTOut`. Every formal Hero/Minion basic-attack State samples `AttackMotionTime`; all formal Idle/Walk/Move states sample `LoopMotionTime`, while Walk/Move playback rate still multiplies live `MoveSpeed` by its per-Controller base normalization. `MobaCameraPresentationConfig` owns animation synchronization (formal 20 Hz), animation interpolation and locked-follow smoothing independently of Gameplay TickRate. Bootstrap publishes its Runtime-owned last completed Tick plus sub-Tick accumulator into a clock isolated by exact `UnitWorld`; outgoing matches cannot leak into or clear a newer match. State/rate/time-regression changes rebuild loop phase from the logic-time epoch. A newly selected ability Stage is routed once through CrossFade, while `IsMoving` changes during an existing movable cast still resolve parameter-driven idle/walk routes before same-frame sampling. A real bound Varus Q regression verifies channel-walk entry, casting-time stop/start routing and continued loop sampling. Attack prediction cannot cross uncommitted Impact or Ready. `PhysicsEntity2D` interpolates position and rotation on independent clocks, and the explicitly late `CameraController` smoothly follows the already projected root; rapid facing changes cannot restart root position progress. `VfxManager.PreloadAsync` warms shared and selected-hero VFX addresses and one inactive pool instance before client event dispatch; VFX 4001 and client projectile bind logs expose owner/selection, cold-load/pool timing for the next rebuilt run. The clocks/samplers, leases and VFX pools remain client-only and absent from Snapshot/checksum/network state. Structure controllers remain explicitly excluded and contain no attack State. Aatrox variants, outline ordering, generic CC arcs and tower Idle/Death behavior remain unchanged. Rebuilt client visual acceptance remains user-owned. |
| UI / Lua | **Lua-driven pages verified; Shop/HUD equipment views corrected** | `UIManager` owns the seven page prefabs and Lua hosts. Shop cells now bind the catalog Sprite, display the formal dynamic purchase price and retain selection through visual owned masks (the complete mask subtree is non-raycast, so purchased cells remain clickable). Selecting an owned catalog entry resolves its lowest occupied slot for details and selling; HUD reads all six equipment slots from `EquipmentHandler`; HeroTest HUD and Shop read the same formal balance instead of the former fake `999999`; HUD and an open Shop refresh from the same post-Tick state so component-consumption marks, recipe discounts and Undo availability do not remain stale. All newly authored equipment and Seething Strike visible text is English because the current TMP font does not provide CJK glyphs. The existing catalog-driven Select, HUD vitals/cooldowns/stats/BuffBar, template-driven player input and minimap boundaries are unchanged. Remaining HUD gaps are production skill-icon sprites, skill-upgrade root, creep-score source and buff tooltips. |
| FrameSync / launch | **D-045 launch + D-053 adaptive Command timing implemented** | Bootstrap owns the two-phase launch schedule and client-only integer Ping estimator; deterministic `FrameSyncGameRuntime` owns no wall-clock timestamp. Ping response v2 anchors SRTT/RTT variation to server Gameplay Tick, while FrameSync consumes only a transport-neutral timing snapshot during new Command creation. A bound, connected client samples throughout the loaded/ready launch barrier, but frozen barrier time is not converted into server Gameplay Tick advance; Command send/recovery/simulation retain their active-game gates. The selected target retains the static synchronized-Tick lower bound, explicit local/server future ceilings and same-build-Tick stability. RTT state never enters Snapshot/checksum/replay. FrameSync EditMode is `132/132`, Bootstrap EditMode is `131/131`; rebuilt multi-process latency acceptance remains pending. |

| External Loading handoff | **Source and focused PlayMode verified; rebuilt client pending** | In externally managed LocalDirect/UOS flow, `GameBootstrap` binds and queues `UIPageId.Load` before the first match-content await, so the standalone GameScene Main fallback cannot flash between Select and Load. The focused test passes with both cold and cached Addressables timing; HUD still opens only at the existing launch barrier. |
| Generic skill indicators | **Atomic Addressables generation ownership and GameScene world-space rendering focused-tested; rebuilt client pending** | HeroTestScene renders the same Varus assets correctly. GameScene's Direction/Range/Ground instances and Aatrox directional-zone lines live under a scene-owned, unparented world-space root, so Camera `LateUpdate` cannot offset or rotate them. On each GameScene content rebind, `ClientContentRuntimeHost` first acquires all new indicator leases and `SkillIndicatorDriver` replaces every generic instance/runtime material clone while the old generation is still resident; only after successful synchronous configuration are the old leases released. This removes both the surviving-old-instance failure and the multi-frame unowned-resource window. Aatrox Q retains its independent LineRenderer/material path and is re-showable after generic rebind. Complete Addressables source `Material` cloning still preserves the resolved Shader, alpha texture, tint and render state without `Shader.Find`; `Client-Shared` remains the common presentation owner and nothing moved into `Logic-Core`. Atomic generation, framebuffer/world-transform and W-then-Q focused PlayMode regressions pass. Final UOS visual acceptance requires the user's rebuilt Windows Client. |

## Playable-logic closure audit

The bootstrap/network chain is closed both locally and through a real UOS
allocation far enough to reach sustained two-client Gameplay. This proves the
composition and authority path, but it does not close every production-facing
acceptance condition: startup queue capacity, the client Loading/HUD timeline,
result/return/remote settlement, Shop content and jungle content remain open.
Detailed continuation evidence is in `CURRENT_HANDOFF.md`.

## Remediation disposition

- No known code-side P0 remains in the already-tested bootstrap/network smoke chain.
- The retained UOS package contained one LocalNGO notification exception, but
  current source subscribes that callback only from the LocalDirect
  `StartClient()` path. Add behavior coverage before claiming the old callback
  remains reachable; a defensive method-level guard is still reasonable.
- The UTP startup send-queue saturation and the client Loading/HUD timing
  observation are open reliability/diagnostic work, not proven deterministic
  Gameplay corruption.
- D-045 source/formal-asset work is complete and focused-tested. Matching rebuilt
  Local C/S and UOS live acceptance is a separate pending request.
- The retained full-suite baseline has 10 EditMode and 4 PlayMode failures;
  these remain visible until their fixtures/expectations are repaired.
- Remaining product work includes jungle content, production presentation/UI
  assets, result/return/remote settlement acceptance and operator visual
  acceptance.
- The incomplete `EquipmentTargetPolicy` design contract is an accepted non-blocking design underspecification, not a guessed production API.

## Permanent constraints

- Unit and Handlers remain prefab-authored MonoBehaviours.
- Reality-time Inspector values use integer milliseconds; other numeric authoring may use `float`, and authoritative Gameplay converts once to Tick/`fp`.
- Bootstrap owns Unity frame scheduling, NGO/UOS, scenes and UI; deterministic assemblies do not depend on those implementations.
- Framework runtime types remain generic; the user-requested Guinsoo equipment
  chain is the explicit formal content exception recorded by D-039.
